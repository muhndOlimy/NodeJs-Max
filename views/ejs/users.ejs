<%- include('includes/head.ejs') %>
<% if(users.length !== 0){ %>
    <h1>Users</h1>
        <ul>
            <% for(let user of users) {%>
                <li><%= user.name%></li>
            <% } %>
        </ul>
    <% } else { %>
        <h1>No users found</h1>
    <% }  %>
<%- include('includes/end.ejs') %>